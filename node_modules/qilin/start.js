var server = require("./server");
var router = require("./router");
var handler = require("./handler");
module.exports = {
	start: function(option){
		server.start(router.route, handler.handler, option);			   
	}	
};

